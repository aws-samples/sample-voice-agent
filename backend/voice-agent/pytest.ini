[pytest]
asyncio_mode = auto
markers =
    integration: marks tests as integration tests requiring AWS credentials (deselect with '-m "not integration"')
    container: marks tests that require container-only dependencies (pipecat, aiohttp, etc.)
